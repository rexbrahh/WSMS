{
  "name": "wsms-pi-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Pi harness extension bridging the agent loop to the WSMS core memory service.",
  "type": "module",
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*",
    "typebox": "*",
    "typescript": "^5.6.0"
  }
}
